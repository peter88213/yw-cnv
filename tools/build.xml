<?xml version="1.0" encoding="UTF-8"?>
<project name="pywriter_lo" basedir=".">
	<property name="release" value="0.24.0" />
	<property name="oxt_file" value="yw-cnv-L-${release}.oxt" />
	
	<property name="oxt" location="../oxt" />
	<property name="src" location="../src" />
	<property name="test" location="../test" />
	<property name="dist" location="../dist" />
	<property name="project-name" value="${ant.project.name}" />
	<property name="release-name" value="${project-name}_v${release}" />

	<target name="build" description="inline all library modules">
		
		<mkdir dir="${test}" />
		<delete dir="${dist}" />
		<delete file="${test}/saveyw.py" />
		<delete file="${test}/openyw.py" />
			<exec executable="python" failonerror="true">
			    <arg value="${src}/build_saveyw.py"/>
			</exec>
			<exec executable="python" failonerror="true">
			    <arg value="${src}/build_openyw.py"/>
			</exec>
	</target>

	<target name="dist" description="generate the distribution">
		
		<mkdir dir="${dist}" />
		<mkdir dir="${dist}/yw-cnv-L-${release}" />
		<mkdir dir="${dist}/yw-cnv-L-${release}/Descriptions" />
		<!--mkdir dir="${dist}/yw-cnv-L-${release}/icons" /-->
		<mkdir dir="${dist}/yw-cnv-L-${release}/META-INF" />
		<mkdir dir="${dist}/yw-cnv-L-${release}/yw-cnv" />
		<mkdir dir="${dist}/yw-cnv-L-${release}/Tooltips" />

		<copy file="${test}/saveyw.py" todir="${dist}/yw-cnv-L-${release}/yw-cnv" />
		<replace file="${dist}/yw-cnv-L-${release}/yw-cnv/saveyw.py" token="@release" value="${release}" />

		<copy file="${test}/openyw.py" todir="${dist}/yw-cnv-L-${release}/yw-cnv" />
		<replace file="${dist}/yw-cnv-L-${release}/yw-cnv/openyw.py" token="@release" value="${release}" />

		<copy file="${oxt}/Descriptions/descr-de.txt" todir="${dist}/yw-cnv-L-${release}/Descriptions" />
		<copy file="${oxt}/Descriptions/descr-en.txt" todir="${dist}/yw-cnv-L-${release}/Descriptions" />
		
		<!--copy file="${oxt}/icons/yWriter_16.png" todir="${dist}/yw-cnv-L-${release}/icons" /-->
		<!--copy file="${oxt}/icons/yWriter_26.png" todir="${dist}/yw-cnv-L-${release}/icons" /-->

		<copy file="${oxt}/META-INF/manifest.xml" todir="${dist}/yw-cnv-L-${release}/META-INF" />
		
		<copy file="${oxt}/Tooltips/tip-en.txt" todir="${dist}/yw-cnv-L-${release}/Tooltips" />
		<copy file="${oxt}/Tooltips/tip-de.txt" todir="${dist}/yw-cnv-L-${release}/Tooltips" />

		<copy file="${oxt}/AddonUI.xcu" todir="${dist}/yw-cnv-L-${release}" />
		<replace file="${dist}/yw-cnv-L-${release}/AddonUI.xcu" token="0.99.0" value="${release}" />

		<copy file="${oxt}/description.xml" todir="${dist}/yw-cnv-L-${release}" />
		<replace file="${dist}/yw-cnv-L-${release}/description.xml" token="0.99.0" value="${release}" />

		<copy file="../README.md" todir="${dist}/yw-cnv-L-${release}" />
		<copy file="../LICENSE" todir="${dist}/yw-cnv-L-${release}" />

		<zip destfile="${dist}/yw-cnv-L-${release}.oxt" basedir="${dist}/yw-cnv-L-${release}" />

		<copy file="./yw-cnv-L.update.xml" overwrite="True" todir=".." />
		<replace file="../yw-cnv-L.update.xml" token="0.99.0" value="${release}" />

	</target>

	<target name="clean" description="clean up">
		
		<delete file="${test}/saveyw.py" />
		<delete file="${test}/openyw.py" />
		
	</target>
	
</project>